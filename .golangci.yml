linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
    - errcheck
    - gosimple
  disable:
    - gosec
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - unused
